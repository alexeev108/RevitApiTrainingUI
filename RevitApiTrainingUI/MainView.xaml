<Window x:Class="RevitApiTrainingUI.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RevitApiTrainingUI"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="200"
        Title="Информация об элементах">
    <StackPanel>        
        <Button Content="Узнать количество труб" Command="{Binding PipesQuantity}" Margin="5"/>
        <Button Content="Узнать объем всех стен" Command="{Binding WallsVolume}" Margin="5"/>
        <Button Content="Узнать количество дверей" Command="{Binding DoorsQuantity}" Margin="5"/>
    </StackPanel>
</Window>
